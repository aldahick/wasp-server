version: 2.1
jobs:
  build:
    docker:
        image: circleci/python:2.7
    steps:
      - checkout
      - run:
          label: Install Python
          command: apk install python
      - run:
          label: Build Docker
          command: docker build -t wasp-server:${CIRCLE_SHA1} .
