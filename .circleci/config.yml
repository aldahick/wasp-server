version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          label: Build Docker
          command: docker build -t wasp-server:${CIRCLE_SHA1} .
